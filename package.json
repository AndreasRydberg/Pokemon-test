{
  "name": "pokemon-battle-simulator",
  "version": "1.0.0",
  "author": {
    "name": "Andreas Rydberg"
  },
  "main": "server.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "dev": "node ./dist/server.js",
    "start": "rm -rf dist && tsc && npm run dev",
    "watch": "rm -rf dist && ts-node-dev --respawn --transpile-only server.ts"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mongodb": "^7.2.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^24.12.4",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5"
  }
}
